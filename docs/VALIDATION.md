# 本次验收记录

日期：2026-09-12。环境：Windows、Node.js 26.1.0、Edge无头浏览器。

## 已执行

- `npm run check` 通过：Prettier、109项单元/API测试、Vite生产构建、2条浏览器端到端流程。
- SQLite持久化重启、修订冲突、校验备份、证据位置与分数类型校验、保存期限清理通过。
- 请求幂等、取消传递、刷新查询、服务重启中断、不发布失败结果通过。
- 简历确认、PDF页码/段落数据映射、中文相关经历选择、覆盖规划、追问停止条件、提示注入引用拒绝通过。
- 浏览器验证完整准备→回答→刷新恢复→报告→换场景复测→历史搜索/重命名/备份路径；第二条独立验证多配置和取消后草稿保留。
- 浏览器检查语音转写文字在确认前不会填入回答；确认后才能提交。未在测试中调用实际麦克风或Web Speech服务。
- 已实际生成打印PDF：`test-results/evidence-report.pdf`；检查打印页包含原文、JD依据和量表信息。
- 已验证360px手机布局，无横向溢出；截图 `test-results/mobile-data.png`。
- `node scripts/check-credentials.mjs` 通过：Windows凭据管理器实际写入、读取、删除；随机测试凭据已删除。
- `node scripts/check-ocr.mjs` 通过：真实Tesseract识别合成PNG，输出 `PROJECT CUSTOMER FEEDBACK`，保留1个原文段落。OCR核心与线程随应用提供，语言包从网络实际加载。
- `git diff --check` 无空白错误。

测试文件与结果使用合成资料。端到端模型供应商为隔离的测试HTTP服务，个人SQLite库未参与这些测试。

## 尚未验收的外部条件

没有实际供应商账户Key，因此尚未执行真实云服务/中转账户的完整验收，也没有评分质量或全部模型兼容性的结论。入口已提供，需在模型设置填入实际账户并确认调用费用。

真实简历扫描件的OCR准确率、实际麦克风和浏览器语音服务、口音识别效果仍取决于运行环境与输入质量，必须由用户校对。合成图片识别通过不代表所有扫描文件都能正确识别。

本轮已增加个人密码和生产访问保护，但没有执行实际公网发布；多人数据隔离和生产出站网络限制仍待后续工作。

## 构建提示

构建仍提示Lucide的`use client`指令被忽略，以及DOCX解析动态包略超过500KB。PDF/OCR解析资源均按需加载；这些提示不导致构建或测试失败。

GitHub Actions已接入相同检查及Chromium浏览器测试；此记录为本机结果，未冒充远端CI已经运行。

## 本轮视频、语言与生产模式

- 已验证 `interviewMode` 支持 `text`、`voice`、`video`，`language` 支持 `zh-CN`、`en-US`、`ja-JP`；非法值在调用模型前拒绝。
- 2026-09-12仅新增了视频代码，当时的2条浏览器测试没有覆盖摄像头录制；不能据此声称全部视频行为已经验收。下方为2026-09-13新增的测试覆盖。
- 已验证 `SERVE_WEB=1` 单进程提供构建前端和 `/api`，本机首页与健康接口返回 200。
- 全量验证：109 项单元/API 测试、2 项浏览器测试、构建和格式检查通过。

## 2026-09-13 上线基础与媒体修复

- 114项单元/API测试：新增个人数据接口认证、Secure Cookie、伪造Cookie拒绝、注销失效、登录限流及过期、生产静态资源和同源写入、备份轮转/损坏拒绝/恢复，以及语言模式完整传递。
- 5项浏览器测试：保留原2条工作流，新增生产构建登录/保存/退出、虚拟摄像头配合真实 MediaRecorder 回放、英文 Speech 配置和校对、切题/离开释放轨道、权限拒绝及迟到权限释放。
- SpeechRecognition 使用测试实现，不代表浏览器供应商转写服务验收；摄像头输入是浏览器虚拟设备，不代表实体设备、移动端浏览器及识别质量。
- Docker CLI存在，但本机 Docker 引擎未运行，尚未构建或启动此镜像。真实供应商、TLS域名与服务器部署亦未执行。
- 功能交付见 [优化进度](OPTIMIZATION-PLAN.md)。

## 2026-09-13 个人长期使用完整优化

`npm run check` 已通过：124项单元/API测试、7项浏览器测试、Prettier检查与Vite生产构建。

- 新增跨模型路由、实际复核模型记录、接收方确认失效、请求幂等、逐模型费率、预算未知/超额拦截和本次覆盖确认。
- 验收矩阵测试确认最新失败覆盖此前成功；测试中的模型服务是兼容测试端点，没有把结果写成真实供应商验收结论。
- 专用ASR测试验证multipart音频请求、接收方戳与上传确认、禁止上传视频类型、不保存音频到任务记录、供应商usage及校对后进入回答。
- 浏览器新增模型分工 → 预算确认 → 独立复核 → 语言引用 → 收藏/岗位模板 → 刷新恢复；视频录制 → 单独音轨转写 → 校对确认链路。
- 掌握度测试覆盖新场景、新回答、间隔、模型变化与勾选不等于掌握；资料原文位置、备份期限清理及打印中的转写/转义已校验。
- 完整加密快照通过数据库恢复、密码哈希/配置/费率/usage还原、错误口令与篡改拒绝、不覆盖目标目录、每日检查及轮转测试。测试用独立口令与临时目录，不含用户资料。
- 发布准备和回滚脚本在临时Git项目中实际执行归档、npm安装/构建及独立数据库恢复，确认原工作目录未变化；这不等于已升级当前部署。

实际云供应商与ASR账户、实体摄像头/麦克风、移动端HTTPS、异地卷连通性、Docker镜像启动及域名部署未由测试替代。当前机器Docker引擎未运行；没有实际供应商Key或部署域名，因此未宣称这些外部验收完成。
