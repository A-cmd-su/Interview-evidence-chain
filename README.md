# Interview Evidence Loop

基于岗位要求与个人经历的在线 AI 面试训练平台。

## 运行

推荐使用最新 Node.js 24 LTS 或 Node.js 26。最低 Node.js 22.13；使用环境网络代理时需升级到支持 `http.setGlobalProxyFromEnv` 的版本。

```bash
npm install
npm run dev
```

`npm run dev` 会同时启动 Vite 前端和本地 API：

- 前端：`http://127.0.0.1:5173`
- API：`http://127.0.0.1:8787`

已有服务占用端口时，可指定独立端口启动，前端代理和本机来源校验会同时使用这些配置：

```powershell
$env:WEB_PORT = "5174"
$env:API_PORT = "8788"
npm run dev
```

预览服务端口可通过 `PREVIEW_PORT` 调整，默认 4173。

## 使用流程

1. 在“模型设置”填写服务商 API 地址、模型 ID 和 API Key，选择 Chat Completions 或 Responses 协议。
2. 可先获取模型列表，也可手动填写。点击“测试连接”，需要进一步验证时主动确认费用并执行“结构化能力测试”，最后保存配置。
3. 粘贴岗位 JD 与简历，也可以导入 TXT、Markdown、文字版 PDF 或 DOCX。
4. 生成岗位能力标签和面试问题。
5. 逐题回答；缺少背景、方案、个人贡献、量化结果或故障兜底时，模型生成针对性追问。
6. 查看证据报告：评分、回答原文、JD 原文和对应量表逐条关联。
7. 打开“整场复盘”，查看各题最新结果、尚未评估的岗位能力与优先训练方向；从关联证据进入单题计划和复测。

## 整场复盘与复测

- 整场复盘只汇总已有在线模型报告，不额外调用模型、不生成新的整场评分。每道主问题只取最新报告，追问不会重复计数，也不会在最新报告缺证时回退展示旧分数。
- 岗位能力显示“有语义复核支持”“待补证据”“历史证据待复核”或“未评估”。复核支持不等于能力已掌握，也不等于已经人工核实。
- 训练优先级按仍有该缺口的主问题数量排序，最多显示三个方向。相同缺口且练习题原文相同的任务去重，保留所选原始任务的报告来源。
- 开始新面试或专项练习前保留上一场的路径快照；当前标签页刷新后仍可回看。旧报告缺少完整路径时明确标记，不推算未作答题目。尚未加入跨设备或长期数据库存储。
- 整场 JSON 导出包括路径、每题最新原始报告、引用关系和任务完成状态。每份原始报告仅保存一次，其他条目使用报告 ID 关联。
- 专项练习不直接与原题比较总分。同题复测仅在岗位、简历、主问题与追问条件、模型、量表、提示词版本和评估配置相同，且双方分数均有完整证据时显示分数对照。模型判断仍可能波动，缺口未再检出不代表已经掌握。

## 证据规则

服务端只接受在线模型结果。模型返回的每一条评分都必须通过三方证据校验：

- 回答中的真实连续片段；
- JD 中的真实连续片段；
- 与评分维度匹配的版本化训练量表。

引用不存在、分数格式不合法或结构不完整时，整份分析请求失败；缺少引用或量表错位时，该维度不发布分数，且不生成总分。不使用本地规则、Mock 结果或旧报告替代失败请求。简历与回答不一致时，只显示“待澄清”，不判断造假。

### 评分语义复核

- 原文校验后，使用当前配置的同一模型发起第二次独立请求，批量判断有效引用是否支撑候选分数和量表档位。复核提供完整 JD、同题回答轮次和候选证据，不发送简历或初评解释。
- 复核结论为支持、不支持或无法判断；后两者保留原文与理由，但不发布维度分数。五维全部通过才生成总分，覆盖率按通过维度计算；不会本地改分。
- 复核必须逐维返回原始证据 ID；漏项、重复、错位、改分、无效 JSON 或网络失败均使整次分析失败，不缓存初评。没有可评分维度时跳过复核，仅保留缺证报告。
- 报告与导出保存复核理由、证据 ID、复核版本及模型配置，不暴露未通过的候选分数。缓存与结构化测试绑定初评及复核版本。历史报告不追溯标记为已复核，也不能直接与新分数比较。
- 两次调用共用设置中的 15–120 秒总时限；通常产生两次模型调用费用，每次明确的 Token 参数拒绝仍最多兼容重试一次。初评与复核并非独立模型，可能共享偏差，不是准确性或真实性保证。追问和训练建议仍来自初评，本阶段只复核五维评分证据。

## 模型兼容

支持 OpenAI-compatible 云服务、中转服务、DeepSeek、通义千问、Kimi、Gemini 兼容接口、Ollama 和 LM Studio。原生 Anthropic Messages 协议暂不支持，Claude 模型需使用提供上述兼容协议的中转。

| 填写地址                                           | 协议      | 实际请求                               |
| -------------------------------------------------- | --------- | -------------------------------------- |
| `https://provider.example`                         | 自动识别  | `/v1/chat/completions`                 |
| `https://provider.example/v1`                      | Responses | `/v1/responses`                        |
| `https://provider.example/responses`               | 自动识别  | `/responses`，保留完整端点             |
| `https://provider.example/api/v1/chat/completions` | 自动识别  | `/api/v1/chat/completions`，不重复拼接 |

自动识别根据完整端点识别协议，只有根地址时默认 Chat Completions；不通过额外付费请求猜测协议。配置页会显示最终请求地址。远程地址必须为 HTTPS，本机服务支持 HTTP。

- Token 参数默认自动兼容。仅在上游以 HTTP 400/422 明确拒绝该参数时，尝试一次另一参数；鉴权、限额、网络错误不自动重试。
- 可手动选择 `max_tokens` / `max_completion_tokens`；Responses 固定使用 `max_output_tokens`。
- 输出上限默认 8192，可调整至 512–32768；超时可调整为 15–120 秒。
- 连接测试发送不含 JD、简历的短请求，最多使用 1024 输出 Token，可能计费。推理预算截断时只确认接口连通并显示警告；正式评分的截断输出仍会被拒绝。
- 获取模型列表通过后端向相同 API 前缀的 `/models` 发起 GET 请求，不需要先填写模型 ID；列表获取失败不影响手填。返回列表不代表这些模型都支持面试分析。
- 结构化能力测试需要单独确认可能计费，只发送内置合成输入，通常调用模型两次，不发送用户的 JD、简历或回答。它覆盖初评与复核结构、证据 ID 校验，不保存测试报告，不使用预置模型输出。无可评分维度时不能通过测试；通过仅表明该次流程与格式有效，不要求复核支持全部分数，也不代表评分准确性或未来请求一定成功。
- 基础连通与结构化测试状态分别记录，并绑定当时配置；配置变化或相应测试失败不会沿用旧的通过状态。报告保存评估配置与提示词版本，不含 Key。
- 保存不等于测试通过。修改配置后须重新测试；Key 不回显、不写入浏览器存储或项目文件，仅保留在本地服务当前会话内存中，会话期限为两小时。

## 连接排查

| 报错                                  | 检查项                                                  |
| ------------------------------------- | ------------------------------------------------------- |
| 应用后端无法连接 / 非 JSON API 响应   | 运行 `npm run dev`，不要只启动前端；确认 8787 端口正常  |
| `ENOTFOUND`                           | API 域名、DNS                                           |
| `ECONNREFUSED`                        | 本机模型是否启动、监听端口是否正确                      |
| `TIMEOUT` / `UND_ERR_CONNECT_TIMEOUT` | 后端网络是否能访问服务商，是否需设置网络代理            |
| HTTP 401 / 403                        | Key、余额对应账户、模型访问权限或供应商拦截             |
| HTTP 404 / 405                        | 最终请求地址、协议、完整模型 ID，避免填入服务商网页地址 |
| HTTP 400 / 422                        | 查看脱敏上游消息，核对 JSON 格式和 Token 参数           |
| HTTP 429                              | 余额、配额或并发限制                                    |

中转 API 地址与网络代理不是同一设置。后端不会自动沿用浏览器或 Windows 的系统代理；有需要时在启动前配置受信任的 HTTP 网络代理，例如 PowerShell：

```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7890"
npm run dev
```

请替换为实际已运行的代理端口。后端读取 `HTTP_PROXY`、`HTTPS_PROXY`、`NO_PROXY`，并始终绕过 localhost、127.0.0.1 和 ::1。没有配置时直接连接。不关闭 TLS 证书校验，不跨域跟随重定向发送 Key。

## 验证

```bash
npm test
npm run build
```

测试中的协议响应仅用于自动化测试，不被应用导入，不提供离线分析或模拟评分入口。真实账户的模型可用性、余额与网络连通性仍需使用用户自己的配置测试。
