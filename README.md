# Interview Evidence Loop

基于岗位 JD、个人经历和原文证据的在线 AI 面试训练平台，面向本机单用户，不限定岗位或技术栈。没有离线评分、模拟报告或失败后回退旧结果的产品路径。

## 启动

使用最新 Node.js 24 LTS 或 Node.js 26；最低 22.13。使用环境网络代理需要支持 `http.setGlobalProxyFromEnv` 的新版本。

```bash
npm ci
npm run dev
```

默认前端 `http://127.0.0.1:5173`，API `http://127.0.0.1:8787`。启动前检查 Node 和端口，不自动终止其他服务。

```powershell
$env:WEB_PORT = "5176"
$env:API_PORT = "8790"
npm run dev
```

前端代理及 API 来源校验同步使用指定端口。`PREVIEW_PORT` 默认 4173，`DATA_DIR` 默认项目下的 `data`。环境变量在同一个终端设置；程序不自动加载 `.env`。

生产构建本机预览：先 `npm run build`，运行 `npm run server`，再在另一个终端执行 `npm run preview`。产品不依赖开发时使用的 Skill 或 MCP。

## 使用流程

1. 在模型设置填写 API 地址、模型 ID 和 Key，测试连接后保存。保存本身不调用模型。
2. 粘贴 JD，导入 TXT、Markdown、PDF 或 DOCX。扫描 PDF/图片先勾选 OCR 再导入。限制 8MB、30页、20000字符。
3. 按需脱敏；校对简历项目、职责、成果、时间线和原文位置。可以让模型提取，也可以按段落手动分类；确认后才出题。
4. 选择基础、标准或进阶难度，确认岗位能力、JD引用、重要性、资历、侧重点、时长。15/30/45/60分钟安排3/5/7/8题。
5. 按岗位覆盖计划回答。追问根据缺口、重复、跳过和剩余时间停止；8轮只是资源保护上限。超时仍保留当前草稿。
6. 查看五维证据、语义复核和待澄清线索。按岗位重要性与历史重复程度排序训练。
7. 勾选只表示做过。原题或不同场景复测依据新回答检查完成标准；不同题目不直接比较总分。
8. “历史与设置”支持搜索、重命名、删除、备份、校验导入、可比条件下的趋势和实际 Token 用量。

## 模型连接

实现 Chat Completions 和 Responses 协议，可配置兼容云服务、中转、本机服务。提供 OpenAI、DeepSeek、通义千问、Kimi、Gemini 兼容接口等地址预设；预设不代表所有模型已通过账户验收。Claude 需经提供这两种协议的服务访问，暂不支持原生 Anthropic Messages。

| 填写地址                                           | 协议      | 最终端点                 |
| -------------------------------------------------- | --------- | ------------------------ |
| `https://provider.example`                         | 自动      | `/v1/chat/completions`   |
| `https://provider.example/v1`                      | Responses | `/v1/responses`          |
| `https://provider.example/responses`               | 自动      | 保留完整端点             |
| `https://provider.example/api/v1/chat/completions` | 自动      | 保留完整端点，不重复拼接 |

远程必须 HTTPS，本机可 HTTP。Token参数默认自动兼容，仅在上游明确拒绝该参数时重试一次；鉴权、配额、网络错误不自动重发。输出上限512–32768，默认8192；初评与复核共享15–120秒时限。

Key 默认在后端会话内存保留两小时。多模型档案可选 Windows 凭据管理器长期保存 Key，不写入浏览器存储、SQLite 或备份。其他平台可使用会话 Key。切换配置后重新确认资料发送。

连接、结构化样例及完整供应商验收分别记录。完整验收需明确确认费用，使用当前真实服务验证连接、提取、出题、评分、追问和长文本。尚未提供实际账户，因此项目没有已通过真实供应商验收的声明。

| 错误                      | 排查                             |
| ------------------------- | -------------------------------- |
| 后端无法连接 / 非JSON响应 | 前端和API同时启动，核对代理端口  |
| `ENOTFOUND`               | 域名和DNS                        |
| `ECONNREFUSED`            | 本机服务和端口                   |
| 超时                      | 网络、中转可用性、代理、输出限制 |
| 401 / 403                 | Key、账户权限、供应商拦截        |
| 404 / 405                 | 协议、最终端点、模型ID           |
| 400 / 422                 | 结构化参数、Token参数            |
| 429                       | 余额、配额、并发限制             |

中转地址和网络代理不是同一设置。程序不自动读取 Windows 代理；如确有可信代理，可在启动前设置：

```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7890"
npm run dev
```

读取 `HTTP_PROXY`、`HTTPS_PROXY`、`NO_PROXY`，本机地址始终绕过代理。不关闭 TLS 校验，也不跨域重定向转发 Key。

## 证据与边界

分数必须关联回答连续原文、JD连续原文和对应版本化量表。伪造引用使请求失败；缺少证据不发布该维度分数。第二次模型请求复核语义，五维均明确支持才产生总分。初评和复核使用同一模型，可能共享偏差，不保证专业判断准确，也不判断经历真假。

岗位覆盖率不代表能力分。趋势按资料、难度、问题路径、模型、量表、评估配置分组；改善是新证据观察，不是招聘结论。知识依据目前为五维通用自建量表，不是专业领域检索知识库。

OCR 在浏览器运行，核心随应用提供，首次需联网下载中英文语言包，识别失败不补写经历。语音使用 MediaRecorder 和浏览器 Web Speech；音频可能发给浏览器供应商，页面需单独确认。自动转写取决于浏览器支持，不支持时可回听并手工校对。只有确认文字进入评分；最多录音3分钟，不分析情绪、性格或表情。

打印版包含原文引用、资料位置、量表、复核和训练任务，可在打印窗口另存 PDF。

## 存储与恢复

默认 SQLite 文件 `data/evidence.sqlite`，使用 WAL。历史不依赖标签页；空数据库可迁移旧标签页记录。并发页面通过修订号检测冲突，未保存时离开页面会提示。

模型任务先持久化请求 ID，刷新查询原任务；同ID同参数不重复调用，成功结果只应用一次。取消传递上游但不保证退款；服务重启的未完成任务标记中断，不自动再次请求。

备份为带 SHA-256 的 JSON，校验结构及危险字段后再导入；校验用于发现损坏，不是来源签名。保存期限默认90天，可改7/30/90/365天或手动删除。启动、每日及修改期限时清理到期资料。删除不等于磁盘安全擦除；外部备份和供应商保存内容需分别管理。

用量来自供应商 `usage`，缺失显示未知，按用户费率估算；不使用字符数冒充 Token。当前用量列表显示最近2000次调用，缓存折扣、税费及未知用量未计入。

当前只绑定本机 loopback，拒绝跨站访问；不应直接通过反向代理公开。公开部署前需补认证、用户数据隔离和严格出站网络限制。详见 [交付说明](docs/OPERATIONS.md) 与 [功能清单](docs/ROADMAP.md)。

## 检查

```bash
npm run format:check
npm test
npm run build
npx playwright install chromium
npm run test:browser
```

Windows 标准路径已安装 Edge 时直接使用 Edge，可通过 `PLAYWRIGHT_CHANNEL`指定；CI使用Chromium。浏览器测试使用独立内存库和测试HTTP供应商，不触碰个人库，不证明真实模型质量。
